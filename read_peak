## this R script for reading peak from the genes

readPeak<-function(Replicate, ...){ 
  peak<-list()
  for(i in 1: nrow(Replicate)){
    peak<-Replicate[i,]
  }
  return(peak)
}


# use lapply function to implement pipe line processing for all peak from the replicates
